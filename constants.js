const constants = {
  testFolder: './files/',
  activeParsers: ['gosUslugi', 'cryptoPro'],
  maximumPing: 3000,
  retryCaptcha: 10,
  puppeteerLaunchOptions: {
    // headless: false,
  },
};

module.exports = constants;
